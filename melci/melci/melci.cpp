#include <fstream>
using namespace std;

int n, p, pozmelca, pozmelcm, treaptaa, treaptam;

int main()
{
    ifstream in("melci.in");
    ofstream out("melci.out");
    bool intalniti = false;
    while (!intalniti)
    {
        pozmelca += 2;
        if (pozmelca >= p)
            treaptaa = pozmelca / p;
        if(treaptaa%2==0)
    }
}